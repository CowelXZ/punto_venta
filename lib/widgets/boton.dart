import 'package:flutter/material.dart';

class BotonNavegador extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: (){},
     child: Text("Hola"));
    // TODO: implement build
  }
}
class letrero_Compra extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return const Text(""

    );
    // TODO: implement build
  }
}